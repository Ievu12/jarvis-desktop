"""Real Connector implementations (jarvis.integrations.base.Connector).
Each module here is one external service. Currently: email (IMAP,
read-only). Future connectors (Stripe, Facebook, Instagram) will live
alongside it, each implementing the same Connector interface."""
